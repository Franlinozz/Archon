export function TestCoverageBar({ category="Reentrancy", covered=3, total=4 }) { const pct=Math.round((covered/total)*100); return <div><div className="mb-2 flex justify-between text-sm"><span className="text-text-mid">{category}</span><span className="font-mono text-text-hi">{covered}/{total} · {pct}%</span></div><div className="h-2 rounded-pill bg-surface-3"><div className="h-2 rounded-pill bg-green-400" style={{width:`${pct}%`}} /></div></div>; }
