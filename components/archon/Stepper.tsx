import { Check, Loader2, X } from "lucide-react";
export type StepState = "completed" | "active" | "queued" | "failed";
export function Stepper({ steps }: { steps: { label: string; state: StepState }[] }) { return <ol className="space-y-3">{steps.map((s)=><li key={s.label} className="flex items-center gap-3"><span className={`grid size-7 place-items-center rounded-full border bg-surface-2 ${s.state==="active"?"border-green-400/50 ring-2 ring-green-400/20 motion-safe:animate-pulse":s.state==="completed"?"border-success/40":"border-border-subtle"}`}>{s.state==="completed"&&<Check size={14} className="archon-pop text-success"/>}{s.state==="active"&&<Loader2 size={14} className="animate-spin text-green-400"/>}{s.state==="failed"&&<X size={14} className="text-danger"/>}{s.state==="queued"&&<span className="size-2 rounded-full bg-text-low"/>}</span><span className={`transition-colors ${s.state==="queued"?"text-text-low":"text-text-hi"}`}>{s.label}</span></li>)}</ol>; }
